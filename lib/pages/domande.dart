import 'package:flutter/material.dart';

class domande extends StatelessWidget {
  const domande({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Card(
        child: Container(
          child: Text("لم يتم تفعيل هذه الخاصية بعد نحن نعمل على ذلك"),
        ),
      ),
    );
  }
}
